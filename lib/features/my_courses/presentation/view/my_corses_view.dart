import 'package:flutter/material.dart';

class MyCorsesView extends StatelessWidget {
  const MyCorsesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('My Corses View'),
      ),
    );
  }
}