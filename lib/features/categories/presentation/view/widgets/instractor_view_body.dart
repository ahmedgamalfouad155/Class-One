import 'package:flutter/material.dart';

class InstractorViewBody extends StatelessWidget {
  const InstractorViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      
    );
  }
}