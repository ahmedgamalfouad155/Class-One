import 'package:flutter/material.dart';

class AdminsScreenBody extends StatelessWidget {
  const AdminsScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [

      ],
    );
  }
}
